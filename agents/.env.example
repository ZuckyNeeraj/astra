# Agents service env. Copy to agents/.env and fill in. NEVER commit real keys.

# Convex — the agents write journey state here.
CONVEX_URL=

# Reasoning + partner integrations (server-side only)
OPENAI_API_KEY=
LINKUP_API_KEY=
ELEVENLABS_API_KEY=
DODO_PAYMENTS_API_KEY=
